W =  (alpha + 1) / (tBlend * (tFinal - tBlend));
radius = 0.08;
line_length = 0.1;
alpha = (2 * line_length) / (radius * 3.1415) + 1;

s = 0.0;
sDot = 0.0;
tBlend = 1.0 ;
tFinal = 4.0;

t = data1.FC50.T10.Time;
N = length(t);
j= 1;

for i=1:N
    [s, sDot] = sFun(W,tBlend,t(j),tFinal);
    [P_z, P_x, P_z_derivative, P_x_derivative] = hypodrome(s,radius,line_length);
    
    t(j)
end

